name: Python CI/CD Deploy

env:
  USERS_TABLE: users

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10.14'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: |
          export PYTHONPATH=$PWD:$PWD/src
          export AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}
          export AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}
          export AWS_DEFAULT_REGION=${{ secrets.AWS_DEFAULT_REGION }}
          export MODELS_TABLE=models-group101-unique-v3
          export AUDIT_TABLE=audit-logs-group101-unique-v3
          export USERS_TABLE=users-group101-unique-v3
          export BUCKET_NAME=project-models-group101-unique-v3
          export SECURITY_HOOK_URL=https://test.hook.svc
          export JWT_SECRET=${{ secrets.JWT_SECRET }}
          export DEFAULT_ADMIN_EMAIL=ece30861defaultadminuser
          export DEFAULT_ADMIN_PASSWORD='correcthorsebatterystaple123(!__+@**(A'"`;DROP TABLE artifacts';
          pytest tests --maxfail=1 --disable-warnings -q

      - name: Deploy to AWS Lambda via SAM
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
        run: |
          pip install aws-sam-cli
          sam build --template template.yml
          # UPDATED TO V3 TO AVOID THE ROLLBACK_COMPLETE ERROR
          sam deploy \
            --stack-name model-registry-v3 \
            --capabilities CAPABILITY_IAM \
            --no-confirm-changeset \
            --resolve-s3 \
            --parameter-overrides \
              JwtSecret=${{ secrets.JWT_SECRET }} \
              ProjectBucketName=project-models-group101-unique-v3 \
              ModelsTableName=models-group101-unique-v3 \
              UsersTableName=users-group101-unique-v3 \
              AuditLogTableName=audit-logs-group101-unique-v3
